<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_invoice_tree_inherit_amounts" model="ir.ui.view">
        <field name="name">account.move.tree.inherit.amounts</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_out_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='amount_total_in_currency_signed']" position="after">
                <field name="amount_paid" string="Paid Amount" widget="monetary" optional="show" sum="Total Residual"/>
                <!-- <field name="amount_residual_signed" string="Remain Amount" widget="monetary" optional="show" sum="Total Residual"/> -->
            </xpath>
        </field>
    </record>
</odoo>