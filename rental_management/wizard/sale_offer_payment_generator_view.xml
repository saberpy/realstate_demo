<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- <record id="sale_offer_payment_generator_form_view" model="ir.ui.view">
        <field name="name">property.sale.offer.payment.generator.form</field>
        <field name="model">property.sale.offer.payment.generator</field>
        <field name="arch" type="xml">
            <form string="Generate Payment Schedule">
                <sheet>
                    <group>
                        <group>
                            <field name="offer_id"/>
                            <field name="currency_id" invisible="1"/>
                            <field name="base_amount" widget="monetary"/>
                        </group>
                        <group>
                            <field name="generated_total" widget="monetary" readonly="1"/>
                            <field name="remaining_amount" widget="monetary" readonly="1"/>
                            <field name="replace_existing"/>
                        </group>
                    </group>
                    <separator string="Payment Rules"/>
                    <p class="text-muted">Create a one-time rule for down payments or milestones, and one recurring rule for monthly, quarterly, or yearly installments.</p>
                    <field name="rule_ids">
                        <list editable="bottom">
                            <field name="sequence" widget="handle"/>
                            <field name="name"/>
                            <field name="calculation_type"/>
                            <field name="percent" invisible="calculation_type != 'percent'" required="calculation_type == 'percent'"/>
                            <field name="fixed_amount" widget="monetary" invisible="calculation_type != 'fixed'" required="calculation_type == 'fixed'"/>
                            <field name="start_date"/>
                            <field name="recurrence"/>
                            <field name="installment_count"/>
                            <field name="description" optional="hide"/>
                            <field name="currency_id" column_invisible="1"/>
                        </list>
                    </field>
                </sheet>
                <footer>
                    <button name="action_generate" type="object" string="Generate Schedule" class="btn-primary"/>
                    <button string="Cancel" special="cancel" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record> -->
    <record id="sale_offer_payment_generator_action" model="ir.actions.act_window">
        <field name="name">Generate Payment Schedule</field>
        <field name="res_model">property.sale.offer.payment.generator</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="sale_offer_payment_generator_form_view"/>
        <field name="target">new</field>
    </record>
</odoo>
